---
name: 企业供应链数智化协同平台
description: 面向采购闭环的专业、精确、克制且可信的企业工作台
---

# Design System: 企业供应链数智化协同平台

## Overview

**Creative North Star：作战台账。** 界面像采购团队共同维护的一本精密台账：对象、状态、数字、责任人和下一步动作紧密相连，任何结论都能下钻到真实业务记录。整体浅色、低疲劳、高密度但不拥挤；排版、对齐和边框承担层级，信号色只把注意力引向需要处理的事项。

工作台拒绝营销落地页、通用 CRUD 皮肤、装饰性 AI 光效和驾驶舱堆卡。主内容最大宽度 1540px，桌面侧栏 250px；1180px 以下指标变两列，860px 以下侧栏抽屉化，640px 以下指标与表单转单列。动效只用于加载、状态反馈和侧栏切换，并尊重减少动态效果设置。

## Colors

所有前端设计色使用 OKLCH；实际 token 位于 `frontend/src/styles/tokens.css`。

| 语义 | Token | 值 | 用途 |
|---|---|---|---|
| 主文字 | `--lm-ink` | `oklch(26% 0.025 92)` | 标题、关键数值 |
| 次文字 | `--lm-ink-soft` | `oklch(43% 0.025 92)` | 正文、导航 |
| 辅助文字 | `--lm-muted` | `oklch(56% 0.025 92)` | 标签、说明 |
| 边框 | `--lm-border` | `oklch(88% 0.018 92)` | 表格、表单、分组 |
| 纸面 | `--lm-paper` | `oklch(99.2% 0.004 92)` | 页面背景 |
| 次表面 | `--lm-surface` | `oklch(97.7% 0.008 92)` | 表头、分组、空状态 |
| 晚夏橄榄 | `--lm-olive` | `oklch(48% 0.075 112)` | 主按钮、当前导航、品牌锚点 |
| 橄榄深色 | `--lm-olive-deep` | `oklch(38% 0.072 112)` | Hover、登录页侧区 |
| 审签蓝 | `--lm-blue` | `oklch(42% 0.09 250)` | 链接、协同、下钻 |
| 成功 | `--lm-success` | `oklch(45% 0.09 150)` | 已完成、通过 |
| 警告 | `--lm-warning` | `oklch(54% 0.12 70)` | 待处理、降级 |
| 危险 | `--lm-danger` | `oklch(51% 0.14 30)` | 失败、阻断、高风险 |

主色在单屏占比不超过约 10%；红、橙、绿只表达稳定业务语义，不作装饰。状态除颜色外必须同时显示文字或图形。图表颜色与 CSS token 语义一致，不得引入无来源的“实时”数值。

## Typography

字体栈为 `"Microsoft YaHei", "PingFang SC", "Noto Sans CJK SC", Arial, sans-serif`，确保 Windows 答辩环境稳定。页面标题 26px/720，模块标题 16px/700，正文与表格 12–14px，元信息 10–11px；登录页展示标题使用 32–54px，但业务工作台不重复使用 Display 级字号。

金额、数量、百分比和日期启用 tabular numerals，并在可比较列右对齐。标题直接说明任务，说明文字补充口径、来源或下一步，不使用空泛营销语句。任何小于 12px 的文本只用于非关键元信息，核心操作与错误不得依赖微小文字。

## Elevation

默认表面扁平：白色/次表面背景 + 1px 边框 + 5px 小圆角。阴影只表示真实覆盖层，统一使用 `0 12px 34px oklch(22% 0.035 92 / 0.07)`，用于移动侧栏、抽屉、对话框和菜单；卡片、指标和表格不靠阴影表达重要性。圆角 token 为 5px，紧凑标签为 3px，不使用大面积胶囊或漂浮卡片。

## Components

- **应用壳与侧栏**：按角色过滤菜单，当前项使用左侧 2px 橄榄标记；真实 JWT 不允许浏览器内切换角色。
- **页面抬头**：标题、任务导向说明和一组右对齐动作；一个任务区通常只有一个主按钮。
- **指标条**：最多四项，从数据库响应或当前列表派生；每项说明口径，禁止硬编码业务数值。
- **工具栏与数据表**：筛选、刷新和条数在同一行；数字右对齐、横向溢出可滚动、空数据有下一步。
- **状态标签**：圆点 + 文案 + 语义色，覆盖业务后端真实状态和未知状态降级。
- **表单/对话框/抽屉**：标签置顶，必填、错误、并发版本和幂等影响明确；危险动作必须说明后果。
- **预警与错误**：左侧语义色边框，显示原因、影响、建议、请求号；不只显示“失败”。
- **AI 解析预览**：并列展示原文、结构化字段、Schema/业务校验、provider、影响和确认；`NEEDS_INPUT`、`INVALID`、FAIL 或回退结果禁止确认。
- **导入批次**：严格执行预览—查看错误—确认提交，不把预览描述成已导入。
- **预测图表**：同时显示模型、基线、14 日序列、指标、降级原因、数据标签和运行时间；没有真实趋势时显示空状态。

## Do’s and Don’ts

### Do

- 用“现在发生了什么、下一步做什么”组织页面。
- 所有数字说明来源、单位、时间和适用范围。
- 为加载、空数据、失败、无权限、并发冲突和服务降级提供明确反馈。
- 按后端状态机显示允许动作，动作成功后重新读取服务端事实。
- 截图和论文证据只来自当前候选版本，并保留演示/合成数据标签。

### Don’t

- 不使用通用后台模板感、营销式 Hero、玻璃拟态、霓虹渐变或装饰性 AI 光效。
- 不使用虚假实时数字、未经校准的置信宣传或为了好看而伪造趋势。
- 不让前端隐藏按钮替代后端权限，也不允许真实账号本地切换角色。
- 不在接口失败时静默切换假数据；演示回退只能显式开启并全局标记。
- 不把所有内容塞进卡片或同一大屏，不用阴影、颜色和动画代替信息结构。
