# 预测实验执行记录（2026-08-24）

文档编号：SCIC-FORECAST-RUN-20260824  
执行日期：2026-08-24（Asia/Shanghai）  
版本：`0.9.0-UAT-RC2`  
数据标签：`DEMO_SYNTHETIC`  
当前状态：**V0.9 UAT 候选，非最终 V1.0**

> 本记录是可复现候选版本的实验摘要。数据为演示/合成数据，不代表任何企业生产数据，也不构成生产容量、业务收益或上线效果结论。

## 1. 执行口径

| 项目 | 本次记录 |
|---|---|
| 数据类型 | `DEMO_SYNTHETIC`（实验模拟数据） |
| 选择模型 | XGBoost（满足当前选模门槛时使用） |
| 预测点数 | 14 点（固定 14 日预测序列） |
| 验证方式 | 按时间顺序的 3 个扩展验证折；固定 14 日输出 |
| 对照基线 | MA7 |
| 特征/切分约束 | 时间序列顺序切分；不随机打乱；滚动特征只使用预测时点之前的数据 |
| 后处理 | 非负截断，保留 6 位小数，递归生成后续预测 |

模型选择遵循工程口径：只有当 XGBoost 平均验证 MAE 低于 MA7，且 3 个 14 日扩展验证折至少赢 2 折时，才选择 XGBoost。不能仅凭一次测试指标手工切换模型。

## 2. XGBoost 结果

以下为本次已确认的 XGBoost 评估指标：

| 指标 | 结果 |
|---|---:|
| MAE | `1.443933` |
| RMSE | `3.214012` |
| MAPE | `3.165298%` |
| 验证折数 | `3` |
| 预测点数 | `14` |

MAPE 仅在真实需求大于 0 的样本上计算；真实需求为 0 的样本不进入 MAPE 分母。指标保留 6 位小数，避免把展示四舍五入误写成更高精度的结论。

## 3. 证据与边界

- 预测实现：`ai-service/app/forecasting.py`；模型和输出契约：`ai-service/app/models.py`；
- 自动化契约测试：`ai-service/tests/test_forecasting.py`；
- 本记录对应固定 14 日输出和 3 折时间扩展评估口径；原始输入快照、数据哈希和逐折明细如未另行归档，不得由本摘要推算或补造；
- `DEMO_SYNTHETIC` 仅用于开发演示和论文方法验证，不能描述为真实企业生产数据、生产预测准确率或线上收益；
- Docker、MySQL 干净环境和用户个人 UAT 不属于本次实验结论。
