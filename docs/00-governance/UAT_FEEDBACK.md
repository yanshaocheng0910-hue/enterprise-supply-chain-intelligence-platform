# 用户验收测试反馈

当前候选版本：`0.9.0-UAT-RC2` 基线 + 2026-09-20 未发布工作树
状态：本轮后端 54、AI 19、便携 MySQL 164/164、前端构建和四角色当前工作树内部浏览器巡检已通过；用户个人测试尚未开始。当前没有用户反馈条目，不能写成 UAT 已通过。

## 使用规则

每条意见都使用 `UAT-版本-序号`，例如 `UAT-090-001`。界面观感、文案和操作不顺也要记录，不只记录程序报错。没有复现和回归证据的口头修改不能标记完成。

## 单条反馈模板

- UAT 编号 / 提交日期：
- 测试版本 / 环境 / 账号角色：
- 所属需求、页面和接口：
- 问题或建议：
- 复现步骤、期望结果、实际结果：
- 截图或请求号 / 日志路径：
- 类型：缺陷 / 易用性 / 新需求 / 数据 / 文案
- 优先级：阻断 / 高 / 中 / 低
- 复现结论与根因：
- 修改文件及数据库/API/状态机影响：
- 新增或更新测试编号：
- 自动回归结果：
- 文档和论文影响：
- 最终状态 / 用户复验日期：

## 汇总表

| UAT 编号 | 版本 | 功能 | 类型 | 优先级 | 状态 | 回归证据 |
|---|---|---|---|---|---|---|

## 2026-09-20 内部运行态验证（不是用户 UAT）

- 当前工作树下，三服务健康检查通过；Spring Boot 真实 HTTP 调 FastAPI 返回 14 点 XGBoost 预测，标记 `DEMO_SYNTHETIC`，provider 为 `rule`。
- BUYER 浏览器登录后完成总览、预测采纳和采购需求草稿回读；预测状态显示已采纳，草稿编号 `REQ-20260920195144-48E69`。收货和预警页面完成页面级检查；浏览器控制台 0 errors / 0 warnings。
- 临时日志和浏览器快照路径见 `docs/04-testing/TEST_EXECUTION_2026-09-20.md`。这些是内部验收观察，不是用户本人操作，也没有全量覆盖四角色或收货/预警写操作。

## 2026-09-20 用户测试待覆盖项（不是已提交反馈）

以下仅是建议按测试计划实际体验的检查点，不构成用户已反馈或已验收的记录。用户确认发现问题后，再新建 `UAT-版本-序号` 条目并关联相应 `TC-*` 用例。

- 预测建议：查看建议依据和状态；BUYER 采纳后出现待审批采购需求；拒绝后状态可见；重复操作不会重复建需求；其它角色不能写入。参照 `TC-FC-006`、`TC-SEC-004`。
- 分批收货：通知到达后进行部分合格/部分拒收；核对库存和订单数量；未全部合格收齐前无法对账，后续到货可完成订单。参照 `TC-BIZ-005`。
- 预警：查看库存不足、处置并关闭；条件仍存在时不反复弹出；条件解除再发生时重新提示。参照 `TC-WARN-001`。
- 订单流程：已审批计划只能生成一张订单；重试不重复；发运和到达按到货通知推进，不通过订单快捷操作跳状态。参照 `TC-BIZ-006`、`TC-BIZ-007`。
- 权限与数据：复核管理员、采购、供应商、管理者的菜单/操作边界及供应商数据范围；规则 provider 应明确显示为规则解析，不显示成真实大模型。参照 `TC-SEC-001` 至 `TC-SEC-004`。
- 初始化范围：若切换合成历史开关，核实其仅影响合成需求历史，不应误以为系统其它演示种子也已清空。参照 `TC-OPS-004`。

以上检查尚未执行；旧 RC2 截图和旧版浏览器巡检不能证明本轮改动已通过用户测试。

## 2026-09-21 内部复核补录（不是用户 UAT）

- 四角色当前工作树浏览器巡检已保存 UI-13—UI-17；最终 BUYER 总览 UI-18 为真实后端连接模式，控制台 0 errors / 0 warnings。
- 便携 MySQL 8.4.11 干净库 164/164 已通过，包括账号停用后旧 JWT 立即失效。此为内部自动化验收，不能代替用户在页面中的操作体验与业务判断。
- 用户个人 UAT 仍为“未开始”。上方待覆盖项是用户操作清单，不因内部测试通过而自动改成已验收。
