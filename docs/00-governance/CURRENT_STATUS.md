# 当前状态

更新时间：2026-09-21（Asia/Shanghai）
当前阶段：V0.9 功能收口候选；用户 UAT 已开始，首批 2 条反馈已修复并等待复验；Docker Compose 环境验收仍未完成
已发布/版本标签基线：`0.9.0-UAT-RC2`；本轮是其上的未发布工作树，不另造已发布版本号
总体状态：Spring Boot 55 项、FastAPI 23 项、真实 MySQL 既有 V1—V5 验收 164/164、前端生产构建和静态检查通过；四服务与本地 Qwen 健康，本地 LLM 混合解析固定集 12/12；本地 AI 经营分析报告已在 H2 运行态生成并留存快照；`UAT-090-001/002` 已内部回归、待用户复验；完整用户 UAT、V6 MySQL 复验和 Docker Compose 容器验收未完成；**非最终 V1.0**

## 2026-09-21 本地 AI 经营分析报告（最新口径）

- 新增只读“供应链经营分析报告”：Spring Boot 汇总订单、库存、供应商、对账和预警事实，FastAPI 不连接数据库；报告不会自动改变采购、库存或对账单据。
- 本地 Qwen 只参与闭集章节风险排序；所有数量、金额、风险事实、行动入口和报告边界由确定性规则生成。模型输出不合规时显式降级为规则报告，不把模型编造内容写入业务库。
- Flyway V6 新增报告快照表，保存统计时点、provider/model/prompt、降级原因、原始事实快照、报告 JSON、SHA-256 数据指纹、幂等键和创建人；生成动作写审计日志。
- 真实运行已生成 `provider=openai-compatible`、`model=qwen2.5:1.5b`、7 个章节和 64 位数据指纹的报告；桌面与 390px 页面通过，无整页横向溢出，控制台 0 errors / 0 warnings。证据为 UI-19、UI-20。
- 最新自动回归为 Spring Boot 55/55、FastAPI 23/23、前端生产构建通过；当前 H2 已迁移到 V6。便携 MySQL 的 164/164 证据仍只覆盖 V1—V5，V6 需另行复验，不能沿用旧数字宣称已覆盖新表。

## 2026-09-21 首批用户反馈修复（最新口径）

- 失效 JWT 不再停留业务页：前端清理旧会话并带原路径返回登录页，登录页明确提示重新登录；通用错误图标尺寸与仓库页错误/空态叠加问题已修复。
- 数据导入改为明确的系统文件选择入口并保留拖放；四类 CSV 模板、服务端预览、错误明细和错误报告下载均为真实功能，不再使用占位提示。
- 修复 Axios 全局 JSON 请求头阻断 multipart 的问题；上传上限前后端统一为 20 MB。浏览器已跑通真实库存 CSV 预览，校验错误由服务端返回且可下载。
- 390px 页面无整页横向溢出，计算字体仍为离线 Noto Sans SC Variable；该轮反馈修复时 Spring Boot 54/54 与前端生产构建通过，当前最新总数见上方经营分析批次。
- 详细反馈、根因和复验状态见 `UAT_FEEDBACK.md`；当前只可称“已修复待用户复验”，不能称用户 UAT 已通过。

## 2026-09-21 本地 AI 与界面收口（最新口径）

- 已接入 Qwen2.5-1.5B-Instruct Q4_K_M 和 llama.cpp b11026。模型、运行时、缓存、日志全部位于 `D:\论文`；模型服务只监听 `127.0.0.1:11435`，无需云端 API 或登录。
- `LLM_LOCAL_ONLY=true` 为默认边界；OpenAI-compatible 地址不是 localhost 时，FastAPI 拒绝发送业务文本并显式规则降级，适用于公司财务/供应链数据不能外发的场景。
- AI 解析记录真实 provider、model、prompt、回退原因和字段来源；模型输出须经过 Schema、原文证据与主数据校验，只形成待确认预览，不能自动下单、入库或对账。
- 固定 12 条本地模型评测首次为 7/12；修复中文量词、标识尾部标点、日期/状态上下文和计划数量语义后，同集回归为 12/12，平均 3266.396 ms。该结果是混合解析链回归，不是裸模型或生产准确率。
- 已真实验证模型不可用时 `provider=rule`、`providerFallback=true`，恢复后可继续本地推理；浏览器 UI-11 证明真实 Qwen 预览和字段来源显示。
- 中文界面统一为随包离线加载的 Noto Sans SC Variable（思源黑体），不使用外部字体 CDN；浏览器计算样式和生产构建已通过，证据为 UI-12。
- 本地解析收口批次复跑为 Spring Boot 54/54、FastAPI 19/19；其后经营分析增量使最新自动化变为 Spring Boot 55/55、FastAPI 23/23。便携 MySQL 8.4.11 的 164/164 仍对应 V1—V5。详情见 `docs/04-testing/TEST_EXECUTION_2026-09-21.md`。
- ADMIN、SUPPLIER、MANAGER、BUYER 当前工作树浏览器巡检及 390px BUYER 页面已留存 UI-13—UI-17；最终 BUYER 工作台复核控制台为 0 errors / 0 warnings。该内部巡检不替代用户个人 UAT。

## 2026-09-20 当前工作树补录（优先口径）

- 架构保持 Vue 3 + Spring Boot 模块化单体 + FastAPI，业务数据库仍由 Spring Boot 唯一写入；本段后续业务闭环内容仍有效，但 AI provider 现状以 2026-09-21 最新口径为准。
- 新增 Flyway V4：预测建议处理状态/版本/说明，预测批次与采纳需求的一对一关联，预警来源键/条件状态，旧需求/计划状态纠正，以及 `purchase_order.plan_id` 唯一约束。
- 已实现预测建议由 BUYER 人工采纳或拒绝；采纳生成可追溯的 `FORECAST` 来源采购需求草稿。库存反馈、拒收反馈和供应商准时率参与确定性建议计算并写入解释；不属于 LLM 判断或模型重训。
- 已实现分批收货：合格数量累计入库，拒收数量不进入合格实收；未全部合格收齐时订单为 `PARTIALLY_RECEIVED` 且不能对账。
- 已实现规则预警来源去重及“人工关闭后仍保持关闭，条件清除后再次出现再重开”的库存预警生命周期；自动化测试验证库存不足用例，其余规则的运行态联调待补。
- 计划到订单按一对一约束；发运/到达使用到货通知工作流；本轮新增后端测试验证精确幂等键及订单状态动作边界。
- 新增 Flyway V5 与采购情景推演：以成功预测批次、库存和未完订单为冻结事实，模拟需求、供应延迟、安全库存、合格率和价格变化，返回基准/模拟逐日库存、建议采购量、金额和风险。推演阶段不改业务单据；两小时内由 BUYER 按版本确认后，才创建 `SCENARIO` 来源的 `DRAFT` 采购需求。模拟与采纳均有幂等键、数据指纹和审计记录。
- `SCIC_DEMO_SYNTHETIC_HISTORY_ENABLED` 仅控制合成需求历史初始化；其它 V2 种子演示数据仍可能存在，不能称作全量清空或纯净生产库模式。
- 本轮执行记录见 `docs/04-testing/TEST_EXECUTION_2026-09-20.md`，新增用例及需求追踪见 `TEST_PLAN_AND_CASES.md`、`TRACEABILITY_MATRIX.md`。

本轮已用 BUYER 登录当前工作树，完成总览、预测采纳与需求草稿回读、收货/预警页面检查，以及采购情景推演的桌面和 390px 窄屏操作；MANAGER 已验证可读取冻结结果但不能生成需求。情景页控制台 0 errors / 0 warnings；详见 `docs/04-testing/TEST_EXECUTION_2026-09-20.md`。旧 RC2 四角色截图仍只代表 RC2。

Sol Max 最终独立复验确认采购情景功能性 P0/P1 为 0；补齐过期审计行和“未到货但余量为零”订单排除的数据库断言后，该功能及当前自动化验收结论为 `PASS`。其后已补做四角色浏览器回归和便携 MySQL 干净库验收；Docker Compose 干净环境复现、用户 UAT 与生产数据实验仍是发布门槛。该结论不等于 V1.0、生产就绪或用户 UAT 通过。

## RC2 基线中已完成并验证（截至 2026-08-25）

- 已建立 Vue 3、Spring Boot、FastAPI 三服务工程及 Flyway V1—V3 数据库迁移。
- 已实现四角色登录、后端授权和供应商数据范围；真实 JWT 不允许在前端直接切换角色。
- 已实现供应商、物料、仓库、库存、四类 CSV 导入、预测运行、采购需求/计划/订单、到货、收货、对账、预警、审计和管理员用户维护。
- 已实现三类受控文本解析，确认前不改变业务事实；规则 provider 明确标注，不伪装成外部大模型。
- 已实现固定 14 天预测、MA7 降级和条件式 XGBoost 选择，输出评估口径与数据标签。
- 已通过 Spring Boot 集成测试 10 项、FastAPI 测试 13 项、前端生产构建和静态交付检查；新增覆盖供应商全局数据隔离、管理员预警写边界、预测物料过滤和 AI 补全确认链。
- 已通过 API 真实跑通 BCL-01 后半闭环：供应商确认、备货、到货通知、发运、到达、收货入库、对账确认与完成；订单终态为 `COMPLETED`。
- 已完成 10 并发、200 次非 AI 看板请求：无非 2xx/5xx，P95 为 88.81 ms（当前本机 H2 环境）。
- 项目缓存、数据库、运行日志和浏览器缓存路径均固定到 D 盘项目目录。
- 已完成 `admin`、`buyer`、`supplier`、`manager` 四角色真实浏览器巡检，保存 7 张候选版截图；全新登录页会话控制台为 0 errors / 0 warnings。
- 已修正预测逐日序列日期映射与物料选择错配，页面读取真实物料、按编码过滤批次，选择器、标题和重算请求一致。

## RC2 历史候选版收口（截至 2026-08-25）

- 实际测试/实验执行记录、追踪矩阵、设计系统和浏览器证据已刷新；
- 候选版页面、接口、角色和演示闭环已具备交接入口；
- Sol Max 首轮独立验收提出的六项阻断已全部关闭；RC2 最终结论为 `PASS WITH NON-BLOCKING LIMITATIONS`、阻断项为 0，可以开始个人 UAT。报告见 `docs/04-testing/SOL_FINAL_REVIEW_2026-08-25.md`。

## 当前尚未完成的发布门槛

1. 四角色当前工作树基础浏览器巡检已完成，但完整的跨角色 UI 写操作、人工体验与异常提示仍需用户个人 UAT。
2. Spring Boot 调 FastAPI 的成功路径和 35 项失败/超时/错误集成场景已自动化覆盖；仍需在用户 UAT 中关注真实长耗时和交互体验。
3. 用户个人 UAT 已开始，首批两条反馈已修复待复验；其余角色和完整业务操作仍需继续按 `UAT_FEEDBACK.md` 登记和回归。
4. 便携 MySQL 8.4.11 干净库已完成 V1—V5 迁移和 164 项独立验收；新增 V6 报告表尚待 MySQL 复验。Docker Compose 容器环境仍未启动实测，不能称容器部署验收通过。
5. 学校 Word/Excel 模板仍保持原样；需求规格、进度计划、设计说明、测试报告、用户手册、实训报告、论文和答辩 PPT 尚未按最终模板冻结。
6. 示例和预测实验数据为 `DEMO_SYNTHETIC`，不能描述为真实企业生产数据。
7. 本地 OpenAI-compatible LLM 已配置并验证；外部云端 LLM 按安全原则不启用。仍需扩大 Ground Truth 与人工修改率统计，不能用 12 条回归集声称生产准确率。

## 下一步

当前先由用户按 `docs/04-testing/TEST_PLAN_AND_CASES.md` 和 `UAT_FEEDBACK.md` 进行个人测试；反馈按“UAT 编号 → 复现 → 修改 → 自动回归 → 用户复验 → 证据更新”处理。具备 Docker 环境后补做 Compose 全新环境启动与代理/健康检查。只有个人 UAT、干净 Compose 复现和最终阻断项全部通过后，才升级为 V1.0。

## 事实边界

- 源码存在不等于全部验收通过；静态 Compose 文件不等于 Docker 已部署。
- 演示数据、规则解析和降级预测必须保留标签。
- 不得先写论文结论再制造指标、截图、文献或实验数据。
- 学校参考模板位于 `D:\lingzhu\Document`，本轮未修改；启用时先复制工作副本到项目 D 盘。
